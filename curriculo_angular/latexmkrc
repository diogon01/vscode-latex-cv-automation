# Configuração de compilação LaTeX
# Diretório de saída para arquivos auxiliares
$aux_dir = 'build';
$out_dir = '.';

# Nome do PDF de saída
$jobname = 'CURRICULO-DIOGO-HENRIQUE-ANGULAR-FULLSTACK';

# Garantir que o diretório build existe
system('if not exist build mkdir build');
